{
  "name": "lru-search-engine",
  "version": "1.0.0",
  "description": "LRU Cache Search Engine with AI Integration",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently \"nodemon server.js\" \"npm run client\"",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "@supabase/supabase-js": "^2.39.0",
    "groq-sdk": "^0.3.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "concurrently": "^8.2.2"
  }
}
